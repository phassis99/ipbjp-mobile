import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.ionic.starter",
  appName: "ipbjp-mobile",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
